{
  "name": "pockit-os",
  "version": "1.0.1",
  "description": "Pockit native OS",
  "main": "dist/index.umd.js",
  "module": "src/index.js",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "prepublishOnly": "npm run build",
    "dev": "nodemon --watch src --watch index.html --ext js,html --exec \"npm run build\""
  },
  "files": [
    "dist",
    "src"
  ],
  "author": "prnth",
  "license": "NITTE",
  "devDependencies": {
    "vite": "^6.3.5"
  },
  "dependencies": {
    "nodemon": "^3.1.10"
  }
}
